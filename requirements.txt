# ============================================================
# requirements.txt — financial-intelligence-platform
#
# Generated from pyproject.toml.  Install everything needed
# to run the application AND the test suite:
#
#   pip install -r requirements.txt
#
# For the app only (no test tooling):
#
#   pip install -r requirements.txt --ignore-requires-python
#   (or just: pip install .)
#
# Minimum Python version: 3.11
# ============================================================

# ------------------------------------------------------------
# Web framework & ASGI server
# ------------------------------------------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.32.0          # httptools + uvloop event loop

# ------------------------------------------------------------
# Database
# ------------------------------------------------------------
sqlalchemy>=2.0.0                  # async ORM (2.x API)
alembic>=1.14.0                    # schema migrations
asyncpg>=0.29.0                    # async PostgreSQL driver (app)
psycopg2-binary>=2.9.10            # sync driver (Alembic migrations only)

# ------------------------------------------------------------
# Configuration & validation
# ------------------------------------------------------------
pydantic-settings>=2.6.0           # Settings class with env-file support
email-validator>=2.0.0             # required by pydantic EmailStr

# ------------------------------------------------------------
# Authentication
# ------------------------------------------------------------
python-jose[cryptography]>=3.3.0   # JWT encode/decode (HS256)
passlib[bcrypt]>=1.7.4             # password hashing
python-multipart>=0.0.9            # form data parsing (OAuth2PasswordRequestForm)

# ------------------------------------------------------------
# ML / data science
# ------------------------------------------------------------
scikit-learn>=1.6.0                # IsolationForest, TF-IDF, LogisticRegression
joblib>=1.4.0                      # model serialisation (used directly in app/ml/)
prophet>=1.1.6                     # time-series forecasting (cmdstanpy backend)
numpy>=1.26.0                      # array ops (used directly in app/ml/anomaly.py)
pandas>=2.2.0                      # DataFrames (used directly across app/ml/ and app/pipelines/)
yfinance>=0.2.50                   # live market data & OHLCV history

# ------------------------------------------------------------
# Infrastructure
# ------------------------------------------------------------
apscheduler>=3.10.4                # cron-based background scheduler (AsyncIOScheduler)
structlog>=24.4.0                  # structured logging (JSON in prod, console in dev)
slowapi>=0.1.9                     # per-IP rate limiting (Starlette middleware)

# ------------------------------------------------------------
# Testing
# ------------------------------------------------------------
pytest>=8.3.0
pytest-asyncio>=0.24.0             # async test functions & fixtures
pytest-cov>=6.0.0                  # coverage measurement (--cov-fail-under=70)
httpx>=0.27.0                      # async HTTP client for FastAPI TestClient
aiosqlite>=0.20.0                  # in-memory SQLite backend for integration tests

# ------------------------------------------------------------
# Linting & formatting (dev only)
# ------------------------------------------------------------
ruff>=0.4.0
